project_root <- "proj_root"

data_raw_dir <- file.path(project_root, "data", "raw")
data_derived_dir <- file.path(project_root, "data", "derived")
output_dir_default <- file.path(project_root, "outputs")
logs_dir <- file.path(project_root, "logs")
run_log_path <- file.path(logs_dir, "run_log.txt")
